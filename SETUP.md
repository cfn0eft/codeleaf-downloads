# コードリーフ BAT起動版（ローカル配布候補）

## 必要な環境

- Windows、Node.js 24以上、公式Salesforce CLI（PATHから利用できること）。
- Microsoft EdgeまたはGoogle Chrome。Git演習はGit for Windowsも必要です。
- Salesforceの実採点にはインターネットと、利用を許可された学習専用Developer Editionが必要です。

## 起動

「必要環境を確認.bat」で、Node.js 24以上・Salesforce CLIの版取得・対応ブラウザの配置を確認し、結果と公式ダウンロードリンクをブラウザに表示します。GitはGit演習用の任意項目として配置のみ確認します。通常の起動BATでも必須環境が不足すれば案内を開きます。Node.js自体がない場合も`requirements.html`から公式ページを開けます。ダウンロードとインストールは公式ページの手順で行い、完了後にBATを開き直してください。

確認結果は日時付きで`requirements.html`へ保存します。URLのパラメーターやJavaScriptは不要です。BATの再実行後、既に開いていた画面は再読み込みしてください。`requirements-template.html`は未確認時の案内原本なので、確認結果を見るときは`requirements.html`を開きます。

1. ZIPを、新しい書き込み可能なフォルダーへ「すべて展開」します。
2. 「コードリーフを起動.bat」を開きます。npm installは不要です。
3. ブラウザに http://127.0.0.1:4317/ が開きます。

黒いウィンドウは使用中は開いたままにします。採点・認証が終わってからCtrl+Cで終了してください。同時に別のコードリーフを起動しないでください。
名前がドットで始まるフォルダー内への展開は避けてください。ZIPの中から直接起動せず、必ず全体を展開します。

## 保存・更新

アプリの「環境・バックアップ」→「アプリの更新」→「更新を確認」で、GitHub Releasesの最新公開版と比較します。自動通信・自動インストールは行いません。新しい版があれば変更内容とZIPダウンロードを表示します。通信失敗時も現在の版で学習できます。配布先: https://github.com/cfn0eft/codeleaf-downloads/releases 。最新版ZIP名は`codeleaf-windows.zip`です。

履歴、下書き、学習用CLI認証は、このアプリの`.local`内に保存します。業務用CLIの通常プロファイルは使用しません。
接続先は本人の学習専用Developer Editionを登録し、公式画面からログインします。
「2. 学習専用組織を接続」の「接続先URL」で、学習用組織のMy Domain（`https://組織名.my.salesforce.com`）を指定できます。Lightning画面のURLもログイン用URLへ変換します。URLを変えたら指定先で「Salesforceで認証」を行い、「接続確認」で実際の組織名・IDを確認します。URLだけでは組織情報は取得できず、認証と3段階登録が必要です。認証先URLはこのPCだけに保存し、学習バックアップへは含めません。
更新は新しいフォルダーへ展開し、旧版の「書き出す」から履歴を保存、新版で復元します。認証・組織登録はやり直します。既存フォルダーへ上書きしないでください。

## 導入済みCLIが見つからない場合

npmによるグローバル導入と公式Windowsインストーラーの標準配置を検索します。
独自の配置では、IT担当者が`CODELEAF_SF_ENTRY`環境変数に公式CLIの`bin/run.js`の絶対パスを指定できます。CLIのJavaScriptをNode.jsから固定引数で実行し、BATや任意のシェルコマンドを採点コードから実行しません。

## 会社PCでの確認

必要な動作はBAT・Node.js・導入済みCLIの実行、PC内の127.0.0.1:4317での待受け、ブラウザ起動、アプリ内`.local`への保存です。Salesforce操作時は登録した学習組織・公式認証先へ通信します。クラウド同期・自動更新・起動時の依存ダウンロードはありません。
管理者権限やセキュリティ設定の変更は行いません。Sophos等がブロックした場合は通知内容をIT担当者へ確認してください。Google Driveでの警告と、PCでの実行制限は別途確認が必要です。

`dependencies.json`に同梱依存と版、各パッケージ内にライセンス、`manifest.json`に配布ファイルのSHA-256を記録しています。ハッシュは改変検出用であり、発行者の電子署名ではありません。
貸与PC・最新版CLIでの動作は実機確認が必要です。同一PCでの検証を別PC成功とは扱いません。
